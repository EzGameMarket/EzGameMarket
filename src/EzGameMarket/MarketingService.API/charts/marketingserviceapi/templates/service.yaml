apiVersion: v1
kind: Service
metadata:
  name: {{ template "marketingserviceapi.fullname" . }}
  labels:
    app: {{ template "marketingserviceapi.name" . }}
    chart: {{ template "marketingserviceapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "marketingserviceapi.name" . }}
    release: {{ .Release.Name }}
